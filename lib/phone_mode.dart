import 'package:flutter/material.dart';

class PhoneMode extends StatelessWidget {
  const PhoneMode({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return ListView();
  }
}
